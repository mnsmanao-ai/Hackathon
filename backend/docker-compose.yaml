version: "3.9"

services:
  frontend:
    build: .
    ports:
      - "80:80"
    depends_on:
      - api

  api:
    build: ./api
    environment:
      DB_HOST: s0c8sws804cgw8c0ko8cg4sw
      DB_USER: hackaton
      DB_PASSWORD: #cfvPQcp%Fi2K0
      DB_NAME: crm_hackathon
      DB_PORT: 3306
    ports:
      - "3307:3307"
    restart: always
